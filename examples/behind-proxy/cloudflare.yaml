http:
  # Add the router
  routers:
    traefik:
      entryPoints:
      - web
      service: traefik
      rule: Path(`/foo`) || Path(`/bar`)

  # Add the service
  services:
    traefik:
      loadBalancer:
        servers:
        - url: http://traefik:80
