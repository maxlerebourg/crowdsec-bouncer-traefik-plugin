http:
  # Add the router
  routers:
    traefik:
      entryPoints:
      - web
      service: traefik
      rule: Host(`localhost`)

  # Add the service
  services:
    traefik:
      loadBalancer:
        servers:
        - url: http://traefik:80
