# Tasks

- [x] Add `countryHeader` and `asnHeader` to `Config` and the bouncer
- [x] Normalize scope, country, and ASN; cache key helpers
- [x] Range index add/remove/match on the shared cache
- [x] Stream ingest by scope; LAPI `scopes=` query
- [x] ServeHTTP lookup: Ip, Range, Country, AS
- [x] Live/none: extra Country/AS LAPI queries when headers are present
- [x] Tests for normalize, range match, header match, stream scopes
- [x] README for the two keys and the four scopes
